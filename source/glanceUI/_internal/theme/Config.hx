package _internal.theme;

import haxe.Json;
import haxe.format.JsonParser;
import flixel.util.FlxSave;

typedef ConfigFile = {
		var mainColor:Int;
		var foreignColor:Int;
		var lineColor:Int;
		var textColor:Int;
		var nameTheme:String;
}

class Config {

	public static var

	public static function init() {

	}

	public static function loadFile(name:String = 'default') {

	}
}
